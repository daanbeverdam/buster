<div class="sidebar card" id="sidebar">
    <div class="sidebar_title">
        <a title="{{@blog.title}}" href='{{@blog.url}}'>
            {{#is 'index'}}
            <h1>
                {{{@blog.title}}}
            </h1>
            {{else}}
            <h2>
                {{{@blog.title}}}
            </h2>
            {{/is}}
        </a>
    </div>
    <div class="sidebar_logo">
    {{#if @blog.logo}}
        <a class="blog_logo" href="{{@blog.url}}"><img src="{{@blog.logo}}" alt="{{@blog.title}}" /></a>
    {{else}}
        <a class="blog_logo" href="{{@blog.url}}"><svg /></a>
    {{/if}}
    </div>
    <div class="sidebar_text">
    <p class="blog_description">
        <a title="{{@blog.title}}" href='{{@blog.url}}'>
            {{@blog.description}}
        </a>
    </p>
    </div>
    {{> footer}}
</div>
