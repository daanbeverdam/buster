{{!< default}}
{{> sidebar}}
{{> header}}
<main class="buster_main" role="main" id="content">
    {{> sidenavigation}}
    {{#foreach posts}}
    <div class="buster_card_wrapper buster_card_index">
    <a href="{{{url}}}" rel="bookmark">
        <article class="buster_card buster_scale" role="article" itemscope itemtype="http://schema.org/Article">
            <div class="buster_card_image">
                {{#if image}}
                    <img src="{{image}}" alt="{{title}}"></img>
                {{else}}
                    <svg alt="{{title}}"/ >
                {{/if}}
                <div class="buster_card_image_filter"></div>
                <div class="buster_card_title">
                    <h2 class="buster_post_title" itemprop="headline">{{title}}</h2>
                </div>
            </div>
            <div class="buster_card_content">
                <div class="buster_post_summary" itemprop="articleBody">
                    <p class="buster_post_excerpt">{{{excerpt words="30"}}}</p>
                </div>
                {{#if tags}}
                    <div class="buster_post_tags">
                        <i class="fa fa-tag buster_no_hover_style" aria-hidden="true"></i> {{tags separator=", "}}
                    </div>
                {{/if}}
                <time class="buster_date" datetime="{{date format="YYYY-MM-DD"}}" itemprop="datePublished">
            </div>
        </article>
    </a>
    </div>
    {{/foreach}}
    {{#if pagination}}
        {{{pagination}}}
    {{/if}}
</main>
<div class="buster_sunscreen" id="sunscreen" onclick="toggle_sidebar()"></div>